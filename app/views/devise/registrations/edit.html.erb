


  <div class="container">
  <div class="row">

    <div class="col-md-4">
      <div class="panel panel-default">
        <div class="panel-heading">1. Datos personales</div>
          <div class="panel-body">

            <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
              
              <%= devise_error_messages! %>

              <div><%= f.label :first_name, label: "Nombre:" %>
              <%= f.text_field :first_name, :autofocus => true %></div><br>

              <div><%= f.label :last_name, label: "Apellido" %>
              <%= f.text_field :last_name, :autofocus => true %></div><br>

              <div><%= f.label :email %>
              <%= f.email_field :email, :autofocus => true %></div><br>

              <% if current_user.role == "user" %>
                
                <div><%= f.label :birthdate, label: "Fecha de nacimiento" %>
                <%= f.text_field :birthdate, :autofocus => true %></div><br>

                <div><%= f.label :gender, label: "Sexo" %>
                <%= f.email_field :gender, :autofocus => true %></div><br>

              <% end %>
            
          </div>
        </div>
      </div>

    <div class="col-md-4">
      <div class="panel panel-default">
        <div class="panel-heading">2. ¿Deseas cambiar tu contraseña actual?</div>
          <div class="panel-body">
            <i>Deja en blanco esta sección si no la quieres modificar</i><br><br>

            <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
              <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
            <% end %>

            <div><%= f.label :password , label: "Contraseña nueva", hint:'Mínimo 8 caracteres.'%>
            <%= f.password_field :password, :autocomplete => "off" %></div><br>

            <div><%= f.label :password_confirmation, label: "Confirma tu nueva contraseña" %>
            <%= f.password_field :password_confirmation %></div><br>

          </div>
      </div>
    </div>


    <div class="col-md-4">
      <div class="panel panel-default">
        <div class="panel-heading">3. Ingresa tu contraseña actual para confirmar los cambios</div>
          <div class="panel-body">

            <div><%= f.label :current_password, "Contraseña actual" %>
            <%= f.password_field :current_password %></div><br>

            <div><%= f.submit "Actualizar mis datos", :class=> 'btn btn-primary' %></div>

            <% end %>

            <% if current_user.role == "user" %>

              <p class="page-header">Quieres cancelar tu cuenta?</p>

              <p><%= button_to "Cancelar mi cuenta", registration_path(resource_name), :class=> 'btn btn-danger', :data => { :confirm => "Te vamos a extrañar!¿Estás seguro que deseas cancelar tu cuenta?" }, :method => :delete %></p>
              
            <% end %>

          </div>
      </div>
    </div>

    <br><br><br>
  </div>


  </div>
</div>



    


